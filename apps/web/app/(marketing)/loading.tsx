// What the arrival shows before its first act can paint.
//
// This is the longest wait on the site and the one most worth dressing. The
// marketing layout mounts Lenis, the concierge nav and the act tracker, and the
// page below it pulls R3F, three and gsap — the whole animation stack the
// funnel's bundle budget exists to keep out of `(booking)`. A guest opening the
// site cold waits on all of it, and what they waited on before this file was a
// blank document.
//
// The line is the arrival's own rather than the funnel's default. "One moment"
// is what a desk says when it is checking something; this is not that, and the
// ride the guest is waiting for is the one the whole page is named after.

import { Threshold } from "@/features/threshold/threshold";

export default function MarketingLoading() {
  return <Threshold label="Arriving" />;
}
