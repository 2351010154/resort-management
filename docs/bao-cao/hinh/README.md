# Hình cho báo cáo đồ án

> ⛔ **LỖI THỜI VÀ KHÔNG TIN CẬY — đánh dấu 2026-08-01.**
> Các `.drawio` ở đây là **bản vẽ tay theo thiết kế**, không sinh ra từ schema,
> migration hay OpenAPI, và không có cổng CI nào phát hiện chúng lệch. ERD, máy
> trạng thái và biểu đồ tuần tự trong thư mục này có thể mâu thuẫn với cơ sở dữ
> liệu và API hiện tại. Không đọc chúng để hiểu hệ thống và không viết mã theo
> chúng; xem [`../../README.md`](../../README.md) để biết owner thật của từng
> loại sự thật.
> *(EN: hand-drawn, stale diagrams for a coursework report. Not generated, not
> verified, not a source of truth.)*

Bản `.png` / `.svg` là kết xuất của `.drawio` cùng tên. Danh mục hình và chương
sử dụng chúng nằm ở [`../README.md`](../README.md).
